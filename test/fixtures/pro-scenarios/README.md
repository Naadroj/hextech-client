# Scénarios golden « pro »

JSON générés par `npm run freeze-scenario` depuis des parties Challenger
moissonnées (`npm run harvest`). Consommés par `scenarios.pro.test.ts`.
Voir `BENCH.md`. Vider ce dossier au changement de patch du snapshot.
